[project]
name = "testing-class"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest",
    "requests>=2.33.1",
]

[dependency-groups]
dev = [
    "pytest-mock>=3.15.1",
]
